Add arch linux iso to this directory: archlinux.iso.

Ensure virtualbox + vbox manager is installed.

Check /media for installer script.

After reboot, the FS may be ro:
```
mount -o remount,rw /
```
